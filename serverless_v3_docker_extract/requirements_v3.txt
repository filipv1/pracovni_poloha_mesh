# V3 Requirements - Compatible with RunPod base image
# RunPod base already has PyTorch 2.0.1+cu118, so we skip it

# RunPod handler
runpod==1.7.3

# Cloud storage (installed separately in Dockerfile)
# boto3==1.34.0
# botocore==1.34.0

# Computer Vision (MediaPipe installed separately for compatibility)
# mediapipe==0.10.8
opencv-python==4.8.1.78
numpy==1.24.3
scipy==1.10.1
scikit-learn==1.3.2

# 3D Processing
# PyTorch is pre-installed in RunPod base image
# torch==2.0.1+cu118
# torchvision==0.15.2+cu118
smplx==0.1.28
trimesh==4.0.8
open3d==0.18.0
chumpy==0.70

# Data Processing
pandas==2.0.3
openpyxl==3.1.2
Pillow==9.5.0
matplotlib==3.7.2

# Utilities
tqdm==4.65.0
pyyaml==6.0.1
click==8.1.7
imageio==2.31.1
imageio-ffmpeg==0.4.9